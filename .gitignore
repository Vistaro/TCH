# Environment & secrets
.env
*.env
config.local.php

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
Thumbs.db
.DS_Store

# Audio / media scratch files
*.mp3
*.wav
*.m4a

# Chat History (local only — never commit)
Chat History/

# Cross-device sync marker written by the SessionEnd backup hook
.last-backup-timestamp

# Brand assets staging
brand.pdf

# Dependencies
vendor/
node_modules/

# Intake parser output — contains PII (personal IDs, contact details, photos)
# The parser script itself IS committed; only its output is excluded.
tools/intake_parser/output/

# Source intake PDFs — contain PII, must not be committed
docs/PDF Imports/
